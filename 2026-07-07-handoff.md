# にげどきチキンレース 引き継ぎ書

- 作成: 2026-07-07（前セッション: Claude Fable 5）/ 更新: 2026-07-08
- 宛先: 本件を引き継ぐ次のセッション（Fable 5 / Sonnet / Codex いずれでも読めるように書く）
- ステータス: **バージョン1 完成（2026-07-08）**。いろいろな人に見せに行くフェーズ。外部レビュー（別セッションの Fable 5 と Codex、および防災士を含む大人たち）進行中

---

## 1. プロジェクト概要（30秒版）

ユーザー（防災士）が防災士フェスタに出す iPad ゲーム「にげどきチキンレース」。避難所準備が流行する中、あえて上流の**避難開始判断**に絞った教育ゲーム。お菓子集め（サンクコスト）を捨てて「にげる!」を押すタイミングを競う。客層はお年寄りと幼稚園〜小学校低学年。機材は iPad 1台のみ。

詳細な経緯・設計判断はすべて文書化済みなので、**このファイルの §2 の順に読めばキャッチアップ完了**。会話履歴を遡る必要はない。

## 2. ファイル一覧（読む順）

すべて `prototypes/nigedoki-game/` 配下:

| ファイル | 役割 |
|---|---|
| `2026-07-07-session-log.md` | 開発セッションの時系列ログ。**最初に読む**（設計判断の経緯と理由が全部ある） |
| `SPEC.md` | レビュー配布用の正式仕様書。目的・分岐確率・フクロウ道場全台詞・新防災気象情報準拠表・レビュー質問9項目 |
| `index.html` | ゲーム本体（単一ファイル、依存ゼロ、オフライン動作） |
| `slides.html` | 展示パネル4枚（A4横・印刷用。大人向け/子ども向け/オタク向け×2） |
| 上位の `prototypes/2026-07-06-festa-nigedoki-game-design.md` | 内部設計メモ（SPEC と重複あり、歴史的経緯用） |
| 上位の `prototypes/2026-07-06-evacuation-timing-tl-game.md` | 別企画: オタク向け偽TLゲーム（ユーザーが手すきで自作予定。フェスタとは別物。触らない） |

## 3. 現在の状態

### 済み（すべて動作検証済み）
- ゲーム全機能: 4ステージ、3種のラン分岐（通常/空振り/晴れのあと災害）、地震ステージ（EEW2.6秒）、フクロウ道場（9系統・3層構成＋タイムライン図）、条件別ランキング、名前選択、説明画面、スタッフ用合言葉ガード
- 令和8年5月29日運用開始の新防災気象情報に用語・色とも準拠（意訳禁止ポリシー）
- 仕様書・展示パネル・レビュー用ログの文書3点
- **バージョン1（2026-07-08）**: 核心メッセージ書き換え（ヘリ救助=運よく間に合った一例）、§1.3 に避難所準備との循環（両輪）追記、緑井の上流ローカライズ、sm13852868 の再解釈（行動の優先順位）、SPEC を v1 に更新（§11 パネル概要新設）。詳細は session-log §8。パネル4枚は A4 内収まりを実測検証済み

### 進行中（次セッションの主業務はこれの受け皿）
- **外部レビュー実施中**: ユーザーが別セッションの Fable 5 と Codex に SPEC.md + session-log を渡してレビューさせている。人間（町内会長・防災士）への試遊も計画中
- レビュー結果が貼られたら: 指摘の妥当性を判断 → index.html / SPEC.md / slides.html に反映 → 再検証

### 未着手（ユーザーと相談してから）
- iPad への配備（GitHub Pages 推奨案は提示済み・未実施。ヴォルトとは別の公開リポジトリに置くこと）
- slides.html の実印刷確認（レイアウトは A4 内収まり検証済みだが、実プリンタ出力は未確認）
- バランス実測（数値はすべて仮。ユーザーの実プレイ感想待ち）
- git コミット（ヴォルトに未コミット変更が大量にある。`/checkpoint` を提案するとよい）

## 4. 絶対に守る設計判断（レビューで覆すには相応の理由が要る）

1. **クイズ形式にしない**。「避難する?」と聞いた時点で負け（表明選好しか測れない）。にげるボタンは常在・無催促
2. **流されたらランキング対象外**。「記録は逃げきった人のもの」が死にゲー化の防止線
3. **空振りランは仕様**（バグではない）。「警報=100%本物」のゲームは現実と逆の学習をさせる。ただし通常ラン多数派（72〜75%）は維持
4. **公式用語・色の厳密準拠**。「高齢者等避難」を「おとしより」等に意訳しない。テレビの実表示との乖離=誤解の危険、がユーザーの強い方針
5. **客層の逃げどきは赤（レベル3）**。紫基準で教えない
6. **失敗を咎める語彙を使わない**（ヴォルト全体の ADHD 配慮と同根。CLAUDE.md 参照）

## 5. 技術メモ

- 起動: `.claude/launch.json` の `nigedoki-game`（port 8931）。`preview_start` で起動し `/index.html`・`/slides.html` を確認できる
- **既知の環境問題**: Claude プレビューパネルのスクリーンショットが派手な演出（EEW点滅・濁流）後に固まることがある。サーバー再起動で直る場合もある。**ゲーム側のバグではない**ので調査に時間を使わないこと。動作検証は `preview_eval` での状態確認が確実
- ルビは `{漢字|かな}` 記法 → `R()` で `<ruby>` 変換。台詞の追加・修正は同記法で
- バランス数値は `index.html` 冒頭の `LEVELS` / `STAGES` 定数に集約
- ランキング: localStorage key `nigedoki-ranking`。スタッフ用全消去の合言葉: 質問「あなたの好きなキャラは?」→ 正解「二号ライダー系」（漢字二/半角2/全角２を許容）
- テスト後は localStorage の記録を必ず消して返すこと（ユーザーの実プレイ記録を汚さない）

## 6. ユーザーとの働き方（このプロジェクトで観測されたこと）

- 設計上の急所を自分で見つけて指摘してくる（死にゲー化懸念、レベル3誤解、確率の偏り等）。**指摘はほぼ毎回正しいので、まず妥当性を認めてから技術的に展開する**とスムーズ
- 「入れてください」と言われるまで実装しない。質問・思考の壁打ちフェーズでは分析だけ返す
- 防災の専門知識がある（防災士）。用語の正確性への要求は高い。曖昧な知識で答えず、公式情報を確認してから反映する（新防災気象情報の時は WebSearch で裏取りした）
- ガンダム・Fate・仮面ライダー・エヴァ等の文脈が通じるし、本人もそれで記憶に定着させるタイプ。ただし**成果物（ゲーム・パネル）に入れるネタはオタク向けシートのみに隔離**するのが確立した運用
- モデル役割分担（CLAUDE.md）: 戦略・レビュー=Fable 5、定型作業=Sonnet（vault-worker）、実装・整備=Codex。このゲームは例外的に Fable が直接実装してきた（創作文言とゲームバランスが密結合のため）

## 7. 次セッションが最初にやること

1. このファイルと `2026-07-07-session-log.md` を読む（済んだらユーザーに一言で現在地を確認）
2. レビュー結果が貼られたら: 指摘ごとに「採用/不採用+理由」を提示 → ユーザー判断を仰いでから反映
3. 反映後は必ず preview で動作検証し、テスト記録を消す
4. 区切りで `/checkpoint`（git コミット）を提案する
